# Shared VecAdd initial evidence

This directory demonstrates the trusted initial-Evidence input accepted by Campaign Bootstrap.
Runtime stores it as epoch-zero context; it is not an authoritative Gateway evaluation and cannot
promote a Kernel. Replace it with operator-specific seed notes or prior evidence in a real
deployment.
