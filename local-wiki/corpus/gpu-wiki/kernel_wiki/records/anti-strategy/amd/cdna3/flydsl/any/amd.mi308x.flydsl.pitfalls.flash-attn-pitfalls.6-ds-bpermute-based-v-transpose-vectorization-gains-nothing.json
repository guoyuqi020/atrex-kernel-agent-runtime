{
 "schema": "clean-1.3",
 "id": "amd.mi308x.flydsl.pitfalls.flash-attn-pitfalls.6-ds-bpermute-based-v-transpose-vectorization-gains-nothing",
 "type": "anti-strategy",
 "level": "cross-operator",
 "status": "active",
 "episode_key": "cdna3|flydsl|flash-attention|flash-attn-pitfalls#6-ds-bpermute-based-v-transpose-vectorization-gains-nothing|cross-operator",
 "retrieval": {
  "scope": {
   "vendor": "amd",
   "arch": "cdna3",
   "architectures": [
    "mi308x"
   ],
   "product": "mi308x",
   "dsl": "flydsl",
   "operator_family": "flash-attention",
   "operators": [
    "flash-attention"
   ],
   "shape_signature": {}
  },
  "generality": {
   "arch": "cdna3",
   "language": "flydsl",
   "workload_family": "attention"
  },
  "signals": {
   "metrics": {},
   "shape_regime": {
    "predicate": "*",
    "var_axes": []
   },
   "symptoms": []
  },
  "technique_tags": [
   "attention",
   "flash-attention"
  ],
  "triggers": [
   "a kernel on AMD MI308X (CDNA3 / gfx942) risks the '6. `ds_bpermute`-based V transpose vectorization gains nothing' trap"
  ]
 },
 "payload": {
  "goal": "Avoid a known trap on AMD MI308X (CDNA3 / gfx942): 6. `ds_bpermute`-based V transpose vectorization gains nothing",
  "problem": {
   "target": "AMD MI308X (CDNA3 / gfx942)",
   "statement": "Pitfalls: FlyDSL Flash Attention on MI308X (gfx942) -- 6. `ds_bpermute`-based V transpose vectorization gains nothing"
  },
  "attempted": "V transpose store is the dominant LDS bank conflict source (2.06 M conflicts ≈ 2.07 M LDS waits per kernel). CK_tile uses `v_perm_b32` (intra-lane byte permute) to vectorize this. FlyDSL doesn't expose `v_perm_b32`, but it does expose `ds_bpermute` (cross-lane gather through LDS). One can swap data with lane (tid XOR 8) so each even-row lane has data for 2 adjacent V rows, then write `ds_write_b32` instead of two scalar `ds_write_b16`. Halves the writes — must be a win.",
  "verdict": "not-worth-it-here",
  "lesson": "`ds_bpermute` has hidden LDS-bandwidth cost that cancels savings from packed writes. `v_perm_b32` (intra-lane) would work in principle but sat in a \"FlyDSL doesn't expose it\" dead-end for months.",
  "established_fact": {
   "condition": {
    "toolchain": "flydsl"
   },
   "mechanism": "`ds_bpermute` is **not free**. It uses the LDS hardware internally for the cross-lane gather. 8 bpermute calls per lane (one per dword to swap an entire v16bf16 vector) ≈ 8 LDS reads worth of LDS-fabric traffic. That cancels the savings from cutting 8 ds_writes per lane. Net LDS pressure is unchanged."
  },
  "observed": "zero perf change.",
  "root_cause": "`ds_bpermute` is **not free**. It uses the LDS hardware internally for the cross-lane gather. 8 bpermute calls per lane (one per dword to swap an entire v16bf16 vector) ≈ 8 LDS reads worth of LDS-fabric traffic. That cancels the savings from cutting 8 ds_writes per lane. Net LDS pressure is unchanged."
 },
 "evidence": {
  "summary": {
   "confidence": "documented"
  },
  "raw": {
   "page_path": "amd/cdna3/mi308x/pitfalls/flydsl/flash-attn-pitfalls.md",
   "seed": "curated markdown wiki, removed after the JSON migration"
  }
 },
 "worth": {
  "rank": {
   "score": 0.45,
   "tier": "cautionary",
   "formula": "clamp(prior + 0.35*signed_feedback*confidence + usage_bonus) * recency_decay(half_life=90d)",
   "components": {
    "prior": 0.45,
    "base_rate": -1.0,
    "feedback_signed": 0.0,
    "feedback_confidence": 0.0,
    "feedback_adjust": 0.0,
    "usage_bonus": 0.0,
    "recency": 1.0
   },
   "computed_at": "2026-08-16T13:27:26",
   "builder_version": "1.1"
  },
  "gain": {
   "basis": "qualitative",
   "kind": "none",
   "pct": null,
   "metrics": []
  },
  "track": {
   "counters": {
    "query_count": 0,
    "served_count": 0,
    "applied_count": 0,
    "verified_effective": 0,
    "verified_ineffective": 0,
    "fallback_served": 0,
    "first_seen_ts": null,
    "last_used_ts": null
   },
   "prior": {}
  }
 }
}
