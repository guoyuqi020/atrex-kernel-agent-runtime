{
 "schema": "clean-1.3",
 "id": "generic.generic.any.kernel-opt.fused-kernel-patterns.pattern-cross-entropy-loss-fusion",
 "type": "technique-card",
 "level": "generic",
 "status": "active",
 "episode_key": "generic|any|hands-on|fused-kernel-patterns#pattern-cross-entropy-loss-fusion|generic",
 "retrieval": {
  "scope": {
   "vendor": "generic",
   "arch": "generic",
   "architectures": [],
   "product": "any",
   "dsl": "any",
   "operator_family": "hands-on",
   "operators": [],
   "shape_signature": {}
  },
  "generality": {
   "arch": "any",
   "language": "any",
   "workload_family": "any"
  },
  "signals": {
   "metrics": {},
   "shape_regime": {
    "predicate": "*",
    "var_axes": []
   },
   "symptoms": []
  },
  "technique_tags": [
   "pattern-cross-entropy-loss-fusion"
  ],
  "triggers": [
   "the 'Pattern: Cross-Entropy Loss Fusion' technique is being considered on GPU (vendor/architecture-general)"
  ]
 },
 "payload": {
  "goal": "Optimization technique for GPU (vendor/architecture-general): Pattern: Cross-Entropy Loss Fusion",
  "problem": {
   "target": "GPU (vendor/architecture-general)",
   "statement": "Fused Kernel Pattern -- Pattern: Cross-Entropy Loss Fusion"
  },
  "technique": "Pattern: Cross-Entropy Loss Fusion",
  "what": "**Source**: `triton-tutorials/08-grouped-gemm.py`, `triton-kernels/`",
  "when": "**Source**: `triton-tutorials/08-grouped-gemm.py`, `triton-kernels/`",
  "caveats": [],
  "success_rate_pct": null,
  "typical_gain_pct": null,
  "implementation": {
   "snippet": "```python\n@triton.jit\ndef cross_entropy_kernel(logits_ptr, labels_ptr, loss_ptr, ...):\n    # 1. Load a row of logits\n    logits = tl.load(logits_ptr + offsets, mask=mask, other=-float('inf'))\n\n    # 2. Online max (numerical stability)\n    max_val = tl.max(logits, axis=0)\n    logits = logits - max_val\n\n    # 3. Compute log(sum(exp(x)))\n    exp_logits = tl.exp(logits)\n    sum_exp = tl.sum(exp_logits, axis=0)\n    log_sum_exp = tl.log(sum_exp)\n\n    # 4. Get the logit corresponding to label\n    label = tl.load(labels_ptr + pid)\n    target_logit = tl.load(logits_ptr + pid * vocab_size + label)\n\n    # 5. loss = log_sum_exp - (target_logit - max_val)\n    loss = log_sum_exp - (target_logit - max_val)\n    tl.store(loss_ptr + pid, loss)\n```",
   "format": "source (verbatim)"
  }
 },
 "evidence": {
  "summary": {
   "confidence": "documented"
  },
  "raw": {
   "page_path": "generic/kernel-opt/hands-on/fused-kernel-patterns.md",
   "seed": "curated markdown wiki, removed after the JSON migration"
  }
 },
 "worth": {
  "rank": {
   "score": 0.55,
   "tier": "provisional",
   "formula": "clamp(prior + 0.35*signed_feedback*confidence + usage_bonus) * recency_decay(half_life=90d)",
   "components": {
    "prior": 0.55,
    "base_rate": -1.0,
    "feedback_signed": 0.0,
    "feedback_confidence": 0.0,
    "feedback_adjust": 0.0,
    "usage_bonus": 0.0,
    "recency": 1.0
   },
   "computed_at": "2026-08-16T13:27:26",
   "builder_version": "1.1"
  },
  "gain": {
   "basis": "qualitative",
   "kind": "none",
   "pct": null,
   "metrics": []
  },
  "track": {
   "counters": {
    "query_count": 0,
    "served_count": 0,
    "applied_count": 0,
    "verified_effective": 0,
    "verified_ineffective": 0,
    "fallback_served": 0,
    "first_seen_ts": null,
    "last_used_ts": null
   },
   "prior": {}
  }
 }
}
