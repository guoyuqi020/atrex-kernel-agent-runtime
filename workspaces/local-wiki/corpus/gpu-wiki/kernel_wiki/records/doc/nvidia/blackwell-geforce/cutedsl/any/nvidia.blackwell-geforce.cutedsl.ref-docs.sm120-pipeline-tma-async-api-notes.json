{
 "schema": "clean-1.3",
 "id": "nvidia.blackwell-geforce.cutedsl.ref-docs.sm120-pipeline-tma-async-api-notes",
 "type": "doc",
 "level": "cross-operator",
 "status": "active",
 "episode_key": "blackwell-geforce|cutedsl|ref-docs|sm120-pipeline-tma-async-api-notes|cross-operator",
 "retrieval": {
  "scope": {
   "vendor": "nvidia",
   "arch": "blackwell-geforce",
   "architectures": [
    "blackwell-geforce"
   ],
   "product": "any",
   "dsl": "cutedsl",
   "operator_family": null,
   "operators": [],
   "shape_signature": {}
  },
  "generality": {
   "arch": "blackwell-geforce",
   "language": "cutedsl",
   "workload_family": "any"
  },
  "signals": {
   "metrics": {},
   "shape_regime": {
    "predicate": "*",
    "var_axes": []
   },
   "symptoms": []
  },
  "technique_tags": [],
  "triggers": [
   "ref-docs reference for NVIDIA Blackwell GeForce/workstation (RTX PRO 5000 / SM120) is needed"
  ]
 },
 "payload": {
  "goal": "Reference for NVIDIA Blackwell GeForce/workstation (RTX PRO 5000 / SM120): cute-DSL `cutlass.pipeline.PipelineTmaAsync` API notes for sm_120 (4.4.2)",
  "problem": {
   "target": "NVIDIA Blackwell GeForce/workstation (RTX PRO 5000 / SM120)",
   "statement": "cute-DSL `cutlass.pipeline.PipelineTmaAsync` API notes for sm_120 (4.4.2)"
  },
  "title": "cute-DSL `cutlass.pipeline.PipelineTmaAsync` API notes for sm_120 (4.4.2)",
  "summary": "This is a reference + a deferred-plan archive. Source-of-truth from spelunking `CUTLASS $CUTLASS_DIR/python/CuTeDSL/cutlass/pipeline/{helpers.py, sm90.py}` while planning a (D) \"abandon warp-spec\" experiment that was ultimately not executed in Stage 3 (V0/V1/V2 already proved the standalone fused-quant ceiling, see ``). The findings are wiki-grade and apply to any future multi-warp TMA design on sm_120 cute-DSL 4.4.2.",
  "sections": [
   {
    "heading": "§1 API contract notes",
    "anchor": "1-api-contract-notes",
    "gist": "`CUTLASS $CUTLASS_DIR/python/CuTeDSL/cutlass/pipeline/helpers.py:46-78`:"
   },
   {
    "heading": "§2 Three feasible (D) \"abandon warp-spec\" designs",
    "anchor": "2-three-feasible-d-abandon-warp-spec-designs",
    "gist": "Background: V2-TMA hit `Achieved Occupancy 18.58 %` because warp-spec (1 producer + 8 consumer warps) limits active warps/sched to 2.26."
   },
   {
    "heading": "§3 When (D) actually matters",
    "anchor": "3-when-d-actually-matters",
    "gist": "(D) was conceived as an exploratory probe to break the standalone-fused-quant 91.9% memcpy ceiling."
   },
   {
    "heading": "§4 Recommendation if (D) probe is run",
    "anchor": "4-recommendation-if-d-probe-is-run",
    "gist": "1."
   },
   {
    "heading": "§5 Wiki value",
    "anchor": "5-wiki-value",
    "gist": "This file's main value is the **API correctness reference** in §1 (precise line ranges in cute-DSL 4.4.2 source) and the **structural finding** that warp-spec is a user convention, not an API requirement."
   },
   {
    "heading": "related",
    "anchor": "related",
    "gist": "- `` — full Stage 3 narrative explaining why (D) was deferred - `` — 5 cute 4.4.2 traps from V2-TMA implementation; will likely apply to (D) implementation too - `wik…"
   }
  ],
  "implementation": {
   "snippet": "```python\nclass CooperativeGroup:\n    def __init__(self, agent: Agent, size: int = 1, ...):\n        if agent is Agent.Thread:\n            assert size > 0\n        ...\n        # Size indicates how many threads are participating in this CooperativeGroup\n        self.size = size\n```\n\n```python\ntidx = tidx % 32\nis_signalling_thread = tidx < cute.size(cluster_shape_vmnk)\n```\n\n```python\nload_pipeline = PipelineTmaAsync.create(\n    barrier_storage=load_mbar_ptr,\n    num_stages=2,\n    producer_group=CooperativeGroup(Agent.Thread, 1),       # any 1 thread issues TMA\n    consumer_group=CooperativeGroup(Agent.Thread, 16*32),   # all 512 threads consume\n    tx_count=tx_count_total,\n    cta_layout_vmnk=cute.tiled_divide(cute.make_layout((1, 1, 1, 1)), (1,)),\n    defer_sync=True,\n)\nprod_state = pipeline.make_pipeline_state(PipelineUserType.Producer, 2)\ncons_state = pipeline.make_pipeline_state(PipelineUserType.Consumer, 2)\nwhile col_chunk < num_col_chunks:\n    if cute.arch.thread_idx()[0] == 0:                  # ← DEADLOCK: breaks elect_one\n        load_pipeline.producer_acquire(prod_state)\n        bar = load_pipeline.producer_get_barrier(prod_state)\n        cute.copy(tma_atom_attn, ..., tma_bar_ptr=bar)\n        cute.copy(tma_atom_gate, ..., tma_bar_ptr=bar)\n        prod_state.advance()\n    load_pipeline.consumer_wait(cons_state)             # all threads wait\n    # ... read SMEM, compute, write fp4\n    load_pipeline.consumer_release(cons_state)          # signalling thread arrives\n    cons_state.advance()\n    col_chunk += 1\n```\n\n```python\nmbar_slot_size = 2 * num_stages   # Int64 entries per warp (32 B per warp)\nwarp_idx = cute.arch.warp_idx()\nwarp_mbar_ptr = base_mbar_ptr + warp_idx * mbar_slot_size\n\nload_pipeline = PipelineTmaAsync.create(\n    barrier_storage=warp_mbar_ptr,                        # per-warp pointer\n    num_stages=2,\n    producer_group=CooperativeGroup(Agent.Thread, 1),     # 1 thread per warp issues\n    consumer_group=CooperativeGroup(Agent.Thread, 32),    # warp's 32 threads consume\n    tx_count=per_warp_tx_count,\n    ...\n)\n# Each warp owns its own (gate_tile_slice, attn_tile_slice) and runs producer + consumer in lockstep.\n```\n\n```python\nPRODUCER_WARPS = 1\nCONSUMER_WARPS = NUM_WARPS - PRODUCER_WARPS\n\nwarp_id = tidx // cutlass.Int32(32)\nis_producer = warp_id == cutlass.Int32(0)\nis_consumer = warp_id >= cutlass.Int32(1)\n\nload_pipeline = pipeline.PipelineTmaAsync.create(\n    barrier_storage=load_mbar,\n    num_stages=1,\n    producer_group=pipeline.CooperativeGroup(pipeline.Agent.Thread, PRODUCER_WARPS),\n    consumer_group=pipeline.CooperativeGroup(pipeline.Agent.Thread, CONSUMER_WARPS),\n    tx_count=tx_count,\n    cta_layout_vmnk=cute.make_layout((1, 1, 1, 1)),\n)\npipeline_init_arrive(cluster_shape_mn=(1, 1))\npipeline_init_wait(cluster_shape_mn=(1, 1))\n\nprod_state = pipeline.make_pipeline_state(pipeline.PipelineUserType.Producer, 1)\ncons_state = pipeline.make_pipeline_state(pipeline.PipelineUserType.Consumer, 1)\n\n# --- producer (warp 0 only) ---\nif is_producer:\n    load_pipeline.producer_acquire(prod_state)\n    bar = load_pipeline.producer_get_barrier(prod_state)\n    cute.copy(tma_atom, tSgA[(None, tile_idx)], tSsA[(None, 0)], tma_bar_ptr=bar)\n    load_pipeline.producer_commit(prod_state)       # NOOP for TMA, but call for API symmetry\n    prod_state.advance()\n\n# --- consumer (warps 1+ only) ---\nif is_consumer:\n    load_pipeline.consumer_wait(cons_state)\n\ncute.arch.barrier()     # ALL warps sync — data is now in SMEM\n\n# --- ALL warps participate in computation / SMEM reads ---\nper_thr = total_elements // THREADS     # must be integer!\nfor i in cutlass.range_constexpr(per_thr):\n    idx = i * cutlass.Int32(THREADS) + tidx\n    # ... read sA[idx], compute, write output ...\n\nif is_consumer:\n    load_pipeline.consumer_release(cons_state)\n    cons_state.advance()\n\ncute.arch.barrier()\n```",
   "format": "source (verbatim)"
  }
 },
 "evidence": {
  "summary": {
   "confidence": "documented"
  },
  "raw": {
   "page_path": "nvidia/blackwell-geforce/ref-docs/cutedsl/sm120-pipeline-tma-async-api-notes.md",
   "seed": "curated markdown wiki, removed after the JSON migration"
  }
 },
 "worth": {
  "rank": {
   "score": 0.4,
   "tier": "provisional",
   "formula": "clamp(prior + 0.35*signed_feedback*confidence + usage_bonus) * recency_decay(half_life=90d)",
   "components": {
    "prior": 0.4,
    "base_rate": -1.0,
    "feedback_signed": 0.0,
    "feedback_confidence": 0.0,
    "feedback_adjust": 0.0,
    "usage_bonus": 0.0,
    "recency": 1.0
   },
   "computed_at": "2026-08-16T13:27:26",
   "builder_version": "1.1"
  },
  "gain": {
   "basis": "qualitative",
   "kind": "none",
   "pct": null,
   "metrics": []
  },
  "track": {
   "counters": {
    "query_count": 0,
    "served_count": 0,
    "applied_count": 0,
    "verified_effective": 0,
    "verified_ineffective": 0,
    "fallback_served": 0,
    "first_seen_ts": null,
    "last_used_ts": null
   },
   "prior": {}
  }
 }
}
